metadata:
  template: lipcoding
  description: "LipCoding - 음성 입력 생산성 앱"

services:
  frontend:
    project: ./frontend
    language: nodejs
    host: appservice
    
  backend:
    project: ./backend
    language: python
    host: appservice

infra:
  path: ./infra
  template: bicep
