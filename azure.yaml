metadata:
  template: lipcoding
  description: "LipCoding - 음성 입력 생산성 앱"

services:
  backend:
    project: ./backend
    language: python
    host: containerapp

infra:
  path: ./infra
  template: bicep
